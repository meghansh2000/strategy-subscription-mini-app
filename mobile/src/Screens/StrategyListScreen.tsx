import { Text, View } from 'react-native';

export default function StrategyListScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Strategy List Screen</Text>
    </View>
  );
}