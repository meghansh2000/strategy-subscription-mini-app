import { Text, View } from 'react-native';

export default function StrategyDetailScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Strategy Detail Screen</Text>
    </View>
  );
}